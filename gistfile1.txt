<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Cake by the Pound — Janet’t Sullivan</title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="styles.css">
  <meta name="description" content="Cake by the Pound — bespoke cakes, catering & sweet treats by Janet’t Sullivan.">
</head>
<body>
  <header class="site-header">
    <div class="container header-inner">
      <a class="logo" href="#">Cake by the Pound</a>
      <nav class="nav">
        <a href="#about">About</a>
        <a href="#menu">Menu</a>
        <a href="#gallery">Gallery</a>
        <a href="#contact">Contact</a>
      </nav>
      <div class="cta">
        <a class="phone" href="tel:+14047166583">Call: (404) 716-6583</a>
      </div>
    </div>
  </header>

  <main>
    <section id="hero" class="hero">
      <div class="container hero-inner">
        <div class="hero-text">
          <h1 class="hero-title">Cake by the Pound</h1>
          <p class="hero-sub">Flowy, soulful cakes & catering — made with love by Janet’t Sullivan</p>
          <div class="hero-actions">
            <a class="btn" href="#menu">See Menu</a>
            <a class="btn ghost" href="#contact">Order Now</a>
          </div>
        </div>
      </div>
    </section>

    <section id="about" class="section about">
      <div class="container">
        <h2>About</h2>
        <p>Hi — I’m Janet’t. I craft custom cakes, desserts, and catering experiences that feel like a warm hug. From intimate celebrations to full catering packages, I design flavors and presentations that make moments unfor:root{
  --bg: #0b0b0b;
  --card: #0f0f0f;
  --accent: #d4a76a; /* warm gold */
  --muted: #bdbdbd;
  --white: #fff;
  --max-width: 1100px;
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family: "Poppins", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  background: var(--bg);
  color:var(--white);
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
  line-height:1.5;
}

.container{max-width:var(--max-width);margin:0 auto;padding:0 20px;}

.site-header{position:sticky;top:0;z-index:50;background:rgba(0,0,0,0.45);backdrop-filter: blur(6px);border-bottom:1px solid rgba(255,255,255,0.03);}
.header-inner{display:flex;align-items:center;justify-content:space-between;padding:14px 0;}
.logo{font-family:"Playfair Display", serif;font-size:20px;color:var(--white);text-decoration:none;}
.nav a{color:var(--muted);text-decoration:none;margin:0 12px;font-size:15px;}
.nav a:hover{color:var(--white)}
.cta .phone{color:var(--accent);text-decoration:none;font-weight:600;}

.hero{padding:80px 0;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0));}
.hero-inner{display:flex;align-items:center;justify-content:flex-start;min-height:320px;}
.hero-title{font-family:"Playfair Display", serif;font-size:48px;margin:0 0 8px;color:var(--white);letter-spacing:0.3px;}
.hero-sub{color:var(--muted);margin:0 0 18px;max-width:720px;}
.btn{display:inline-block;background:var(--accent);color:#111;padding:10px 16px;border-radius:8px;text-decoration:none;font-weight:600;margin-right:10px;}
.btn.ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--white)}

.section{padding:48px 0;}
.section h2{font-family:"Playfair Display", serif;margin:0 0 14px;font-size:28px;}
.muted{color:var(--muted)}

.features{list-style:none;padding:0;margin:10px 0 0;display:flex;gap:16px}
.features li{background:rgba(255,255,255,0.02);padding:10px 14px;border-radius:8px;color:var(--muted);font-size:14px}

.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:16px;margin-top:16px}
.card{background:var(--card);padding:18px;border-radius:12px}
.card h3{margin:0 0 8px;font-size:18px}
.price{color:var(--accent);font-weight:600;margin-top:12px}

.gallery-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:10px;margin-top:16px}
.gallery-grid img{width:100%;height:160px;object-fit:cover;border-radius:10px;filter:saturate(0.95) contrast(1.03)}

.contact-grid{display:grid;grid-template-columns:1fr 1fr;gap:24px;align-items:start}
.contact-form label{display:block;margin-bottom:10px;color:var(--muted);font-size:14px}
.contact-form input, .contact-form textarea{width:100%;padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.06);background:#0b0b0b;color:var(--white)}
.contact-form button{margin-top:8px}

.site-footer{border-top:1px solid rgba(255,255,255,0.03);padding:18px 0;margin-top:30px}
.footer-inner{display:flex;justify-content:space-between;align-items:center;color:var(--muted);font-size:14px}

/* Responsive */
@media (max-width:900px){
  .header-inner{flex-direction:column;gap:12px}
  .contact-grid{grid-template-columns:1fr}
  .hero-title{font-size:36px}
}

:root{
  --bg: #0b0b0b;
  --card: #0f0f0f;
  --accent: #d4a76a; /* warm gold */
  --muted: #bdbdbd;
  --white: #fff;
  --max-width: 1100px;
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family: "Poppins", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  background: var(--bg);
  color:var(--white);
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
  line-height:1.5;
}

.container{max-width:var(--max-width);margin:0 auto;padding:0 20px;}

.site-header{position:sticky;top:0;z-index:50;background:rgba(0,0,0,0.45);backdrop-filter: blur(6px);border-bottom:1px solid rgba(255,255,255,0.03);}
.header-inner{display:flex;align-items:center;justify-content:space-between;padding:14px 0;}
.logo{font-family:"Playfair Display", serif;font-size:20px;color:var(--white);text-decoration:none;}
.nav a{color:var(--muted);text-decoration:none;margin:0 12px;font-size:15px;}
.nav a:hover{color:var(--white)}
.cta .phone{color:var(--accent);text-decoration:none;font-weight:600;}

.hero{padding:80px 0;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0));}
.hero-inner{display:flex;align-items:center;justify-content:flex-start;min-height:320px;}
.hero-title{font-family:"Playfair Display", serif;font-size:48px;margin:0 0 8px;color:var(--white);letter-spacing:0.3px;}
.hero-sub{color:var(--muted);margin:0 0 18px;max-width:720px;}
.btn{display:inline-block;background:var(--accent);color:#111;padding:10px 16px;border-radius:8px;text-decoration:none;font-weight:600;margin-right:10px;}
.btn.ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--white)}

.section{padding:48px 0;}
.section h2{font-family:"Playfair Display", serif;margin:0 0 14px;font-size:28px;}
.muted{color:var(--muted)}

.features{list-style:none;padding:0;margin:10px 0 0;display:flex;gap:16px}
.features li{background:rgba(255,255,255,0.02);padding:10px 14px;border-radius:8px;color:var(--muted);font-size:14px}

.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:16px;margin-top:16px}
.card{background:var(--card);padding:18px;border-radius:12px}
.card h3{margin:0 0 8px;font-size:18px}
.price{color:var(--accent);font-weight:600;margin-top:12px}

.gallery-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:10px;margin-top:16px}
.gallery-grid img{width:100%;height:160px;object-fit:cover;border-radius:10px;filter:saturate(0.95) contrast(1.03)}

.contact-grid{display:grid;grid-template-columns:1fr 1fr;gap:24px;align-items:start}
.contact-form label{display:block;margin-bottom:10px;color:var(--muted);font-size:14px}
.contact-form input, .contact-form textarea{width:100%;padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.06);background:#0b0b0b;color:var(--white)}
.contact-form button{margin-top:8px}

.site-footer{border-top:1px solid rgba(255,255,255,0.03);padding:18px 0;margin-top:30px}
.footer-inner{display:flex;justify-content:space-between;align-items:center;color:var(--muted);font-size:14px}

/* Responsive */
@media (max-width:900px){
  .header-inner{flex-direction:column;gap:12px}
  .contact-grid{grid-template-columns:1fr}
  .hero-title{font-size:36px}
}


// Small interactive bits: contact form triggers mailto, updates copyright year
document.getElementById('year').textContent = new Date().getFullYear();

const form = document.getElementById('contact-form');
form.addEventListener('submit', function(e){
  e.preventDefault();
  const name = encodeURIComponent(form.name.value.trim());
  const email = encodeURIComponent(form.email.value.trim());
  const message = encodeURIComponent(form.message.value.trim());
  const subject = encodeURIComponent('Website inquiry from ' + (name || email));
  const body = `Name: ${name}%0D%0AEmail: ${email}%0D%0A%0D%0A${message}`;
  // opens user's email client to send the inquiry
  window.location.href = `mailto:cakebythepound1111@gmail.com?subject=${subject}&body=${body}`;
});